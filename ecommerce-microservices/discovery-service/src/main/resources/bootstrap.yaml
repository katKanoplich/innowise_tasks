spring:
  application:
    name: discovery-service
  config:
    import: 'optional:configserver:'
  cloud:
    config:
      enabled: true
      allow-override: true
      uri: http://localhost:8888
      name: discovery-service

